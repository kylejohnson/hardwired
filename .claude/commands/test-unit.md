---
description: Run pytest unit tests
allowed-tools: Bash(uv run pytest tests/unit/)
---

Run the unit test suite: `uv run pytest tests/unit/`

If tests fail, or there are warnings, analyze the output and suggest fixes.